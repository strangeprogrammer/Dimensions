#include <all.hpp>

int main(){
	//Open file as 'fp' and check for errors
	//dup2 'stdout' as 'stdoutbkp'
	//dup2 'fp' as 'stdout'
	//Create a data structure for the program to run upon and check for errors
	//Create a parser wrapper and check for errors
	//Call the parser to create an execution chain and check for errors
	//dup2 'stdoutbkp' to 'stdout'
	//Call 'execute' upon the execution chain and the data structure and store the return value
	//Delete the data structure, execution chain, and parser wrapper
	//Print the return value
	return 0;
}
