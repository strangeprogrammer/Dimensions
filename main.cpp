#include <all.hpp>

int main(){
	//Open file as 'fp' and check for errors
	//dup2 'stdout' as 'stdoutbkp'
	//dup2 'fp' as 'stdout'
	//Create a data structure for the program to run upon
	//Call the parser wrapper
	//
	//dup2
	
	return 0;
}
